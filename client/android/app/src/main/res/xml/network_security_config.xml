<?xml version="1.0" encoding="utf-8"?>
<!--
  Production: HTTPS-only (default). Cleartext is denied to all hosts not
  listed below so a release build can never accidentally talk over HTTP.

  Dev exceptions: emulator loopback, LAN dev servers, and explicit local
  IPs we use for testing. Keep this list narrow.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.3.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.10.100</domain>
    </domain-config>
</network-security-config>
